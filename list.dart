void main(){
  List<int> ls=[1,2,3,4,5];
 // ls.add(6);
 
 // print(ls.first);
 //print(ls.isEmpty);
 //print(ls.isNotEmpty);
 //print(ls.length);

 //print(ls.indexOf(5));
 //print(ls.reversed.toList());
 print(ls.removeAt(3));
}